# Firebase Admin - uma das opções abaixo:

# Opção 1 (local): caminho para o JSON da service account
# GOOGLE_APPLICATION_CREDENTIALS=./beachflow-system-firebase-adminsdk-xxxxx.json

# Opção 2 (Render/produção, recomendado): JSON da service account em Base64 (evita erro DECODER routines::unsupported)
# No terminal: node -e "console.log(require('fs').readFileSync('./caminho/para/serviceAccount.json').toString('base64'))"
# FIREBASE_SERVICE_ACCOUNT_BASE64=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ii...

# Opção 3: JSON completo em uma linha (menos recomendado que Base64)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Opção 4: variáveis separadas (no Render a chave pode corromper; prefira Base64)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Opcional: Realtime Database (status das quadras em tempo real)
# FIREBASE_DATABASE_URL=https://SEU-PROJECT-ID-default-rtdb.firebaseio.com

# Server
PORT=4000
FRONTEND_URL=http://localhost:5173

# Opcional: email (Nodemailer/SendGrid)
# SENDGRID_API_KEY=
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# Opcional: pagamentos
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# PAGSEGURO_EMAIL=
# PAGSEGURO_TOKEN=
